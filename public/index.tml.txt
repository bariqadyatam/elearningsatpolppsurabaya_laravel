<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <title>Sertifikat</title>
    <style>
        @page { margin: 0px; }
        body {
            margin: 0px;
            padding: 0px;
            background-image: url('{{ public_path("assets/sertifikat/hasil.png") }}');
            background-size: cover;
            background-repeat: no-repeat;
            font-family: Arial, sans-serif;
            position: relative;
            width: 100%;
            height: 100%;
        }

        .nama {
            position: absolute;
            top: 330px; /* atur sesuai kebutuhan */
            width: 100%;
            text-align: center;
            font-size: 32px;
            font-weight: bold;
            color: #000000;
        }

        .materi {
            position: absolute;
            top: 390px;
            width: 100%;
            text-align: center;
            font-size: 20px;
            color: #333;
        }

        .tanggal {
            position: absolute;
            bottom: 140px;
            width: 100%;
            text-align: center;
            font-size: 16px;
            color: #333;
        }
    </style>
</head>
<body>
    <div class="nama">{{ $nama }}</div>
    <div class="materi">{{ $judul_materi ?? 'Telah Mengikuti e-Learning' }}</div>
    <div class="tanggal">Surabaya, {{ \Carbon\Carbon::parse($tanggal)->translatedFormat('d F Y') }}</div>
</body>
</html>
